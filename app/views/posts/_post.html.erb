<% if current_user.liked_by?(@post.id) %>
  <td><%= link_to 'いいねを外す', post_like_path(@post), method: :DELETE, remote: true %> <%= @post.likes.count %></td>
<% else %>
  <td><%= link_to 'いいね', post_likes_path(@post), method: :POST, remote: true %> <%= @post.likes.count %></td>        
<% end %>